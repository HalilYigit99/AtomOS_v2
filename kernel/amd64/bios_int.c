#include <arch.h>

void amd_bios_int(uint8_t int_no, arch_processor_regs_t* in, arch_processor_regs_t* out)
{
    // BIOS interrupt handling code for AMD64 would go here.
    // This is typically implemented in assembly language due to the low-level nature of BIOS interrupts.
}
